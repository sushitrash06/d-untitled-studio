
import { Roboto_Mono, Nokora } from 'next/font/google';

export const robotoMono = Roboto_Mono({
  subsets: ['latin'],
  weight: ['400'],
});

export const nokora = Nokora({
  subsets: ['latin'],
  weight: ['400', '700'],
});
